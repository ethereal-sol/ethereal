<!doctype html>
<html lang="ru">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
  <meta name="color-scheme" content="dark">
  <title>Ethereal</title>

  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&display=swap" rel="stylesheet">

  <style>
    :root{
      --bg:#050607;
      --text:rgba(255,255,255,.92);
      --muted:rgba(255,255,255,.62);
      --radius:22px;
      --shadow:0 18px 55px rgba(0,0,0,.72);
      --mono:ui-monospace,SFMono-Regular,Menlo,Monaco,Consolas,"Liberation Mono","Courier New",monospace;
      --sans:ui-sans-serif,system-ui,-apple-system,Segoe UI,Roboto,"Helvetica Neue",Arial,"Noto Sans","Liberation Sans",sans-serif;
      --space:"Space Mono",var(--mono);
      --glass-blur:14px;
      --glass-blur-soft:10px;
      --frame-top:rgba(0,0,0,.62);
      --frame-bottom:rgba(0,0,0,.54);
      --card-top:rgba(2,2,4,.58);
      --card-bottom:rgba(0,0,0,.50);
      --spec-top:rgba(255,255,255,.20);
      --spec-bottom:rgba(0,0,0,.32);
      --spec-inner:rgba(255,255,255,.022);
    }

    *{box-sizing:border-box}

    html,body{
      width:100%;
      min-height:100%;
      overflow-x:clip;
    }

    @supports not (overflow:clip){html,body{overflow-x:hidden}}

    body{
      margin:0;
      color:var(--text);
      font-family:var(--sans);
      background:
        radial-gradient(1200px 800px at 70% 20%,rgba(255,255,255,.028),transparent 60%),
        radial-gradient(900px 700px at 20% 80%,rgba(255,255,255,.018),transparent 64%),
        var(--bg);
      overflow-y:auto;
      -webkit-text-size-adjust:100%;
    }

    #bg,.vignette,.vhs,.vhs-glitch{
      position:fixed;
      inset:0;
      pointer-events:none;
    }

    #bg{
      z-index:0;
      width:100%;
      height:100%;
    }

    .vignette{
      z-index:1;
      background:radial-gradient(circle at 50% 45%,transparent,rgba(0,0,0,.62) 70%,rgba(0,0,0,.84));
    }

    .stage{
      position:relative;
      z-index:2;
      min-height:100svh;
      display:flex;
      justify-content:center;
      align-items:flex-start;
      --pad:clamp(16px,3vw,40px);
      padding:
        calc(var(--pad) + env(safe-area-inset-top))
        max(16px,env(safe-area-inset-right))
        calc(var(--pad) + env(safe-area-inset-bottom))
        max(16px,env(safe-area-inset-left));
    }

    .stack{
      width:min(980px,100%);
      display:flex;
      flex-direction:column;
      gap:14px;
    }

    .frame,.card{
      position:relative;
      isolation:isolate;
      overflow:hidden;
      border:1px solid transparent;
      border-radius:var(--radius);
      background-repeat:no-repeat;
      background-origin:padding-box,border-box;
      background-clip:padding-box,border-box;
      contain:layout style paint;
      box-shadow:
        var(--shadow),
        inset 1.5px 1.5px 0 -1px var(--spec-top),
        inset -1px -1px 1px -1px var(--spec-bottom),
        inset 0 0 22px var(--spec-inner);
    }

    .frame{
      width:100%;
      margin:0 auto;
      background:
        linear-gradient(180deg,var(--frame-top),var(--frame-bottom)) padding-box,
        linear-gradient(90deg,rgba(255,255,255,.025) 0 22%,rgba(255,255,255,.08) 28%,rgba(255,255,255,.22) 34%,rgba(140,140,140,.14) 40%,rgba(255,255,255,.07) 47%,rgba(255,255,255,.025) 54% 100%) border-box;
      background-size:100% 100%,240% 100%;
      background-position:0 0,140% 0;
      animation:borderSweep 9s cubic-bezier(.45,.05,.55,.95) infinite;
    }

    .hero{
      opacity:0;
      transform:translateY(18px) scale(.985);
      animation:
        frameIn .9s cubic-bezier(.16,.84,.28,1) .1s forwards,
        borderSweep 9s cubic-bezier(.45,.05,.55,.95) 1.2s infinite;
    }

    .card{
      min-height:124px;
      padding:16px;
      background:
        linear-gradient(180deg,var(--card-top),var(--card-bottom)) padding-box,
        linear-gradient(90deg,rgba(255,255,255,.022) 0 24%,rgba(255,255,255,.06) 30%,rgba(255,255,255,.16) 35%,rgba(110,110,110,.10) 41%,rgba(255,255,255,.05) 48%,rgba(255,255,255,.022) 55% 100%) border-box;
      background-size:100% 100%,250% 100%;
      background-position:0 0,140% 0;
      transition:transform .35s cubic-bezier(.16,.84,.28,1);
      animation:borderSweep 10s cubic-bezier(.45,.05,.55,.95) infinite;
    }

    .card:hover{transform:translateY(-3px)}

    .frame::before,.card::before,.btn::before{
      content:"";
      position:absolute;
      inset:0;
      z-index:0;
      border-radius:inherit;
      pointer-events:none;
      backdrop-filter:blur(var(--glass-blur)) saturate(115%) contrast(108%) brightness(82%);
      -webkit-backdrop-filter:blur(var(--glass-blur)) saturate(115%) contrast(108%) brightness(82%);
    }

    .card::before{--glass-blur:var(--glass-blur-soft)}

    .frame::after,.card::after{
      content:"";
      position:absolute;
      inset:0;
      z-index:0;
      border-radius:inherit;
      pointer-events:none;
      background:
        radial-gradient(160% 90% at -5% -20%,rgba(255,255,255,.08),transparent 44%),
        radial-gradient(120% 60% at 110% -10%,rgba(255,255,255,.035),transparent 46%),
        radial-gradient(130% 90% at 50% 130%,rgba(0,0,0,.55),transparent 55%),
        linear-gradient(180deg,rgba(255,255,255,.015),rgba(0,0,0,.08) 40%,rgba(0,0,0,.22));
      opacity:.92;
    }

    .card::after{
      background:
        radial-gradient(140% 80% at -5% -10%,rgba(255,255,255,.06),transparent 46%),
        radial-gradient(110% 55% at 110% -10%,rgba(255,255,255,.025),transparent 48%),
        radial-gradient(120% 80% at 50% 130%,rgba(0,0,0,.48),transparent 56%),
        linear-gradient(180deg,rgba(255,255,255,.012),rgba(0,0,0,.06) 38%,rgba(0,0,0,.18));
      opacity:.9;
    }

    .frame-inner,.card>*{
      position:relative;
      z-index:1;
    }

    .frame-inner{
      padding:clamp(18px,3vw,34px);
      border-radius:inherit;
    }

    @keyframes frameIn{
      0%{opacity:0;transform:translateY(18px) scale(.985);filter:blur(6px)}
      60%,100%{filter:blur(0)}
      100%{opacity:1;transform:translateY(0) scale(1)}
    }

    @keyframes contentIn{
      from{opacity:0;transform:translateY(12px)}
      to{opacity:1;transform:translateY(0)}
    }

    @keyframes borderSweep{
      0%,100%{background-position:0 0,140% 0}
      55%{background-position:0 0,-40% 0}
      55.01%{background-position:0 0,140% 0}
    }

    .hero .logo,.hero .sub,.hero .divider,.hero .actions>.btn{
      opacity:0;
      animation:contentIn .7s cubic-bezier(.16,.84,.28,1) forwards;
    }

    .hero .logo{animation-delay:.35s}
    .hero .sub{animation-delay:.5s}
    .hero .divider{animation-delay:.6s}
    .hero .actions>.btn:nth-child(1){animation-delay:.7s}
    .hero .actions>.btn:nth-child(2){animation-delay:.82s}
    .hero .actions>.btn:nth-child(3){animation-delay:.94s}

    .logo{
      margin:0 0 6px;
      display:flex;
      align-items:center;
      justify-content:flex-start;
    }

    .logo-svg{
      display:block;
      width:min(560px,100%);
      height:clamp(34px,3.4vw,52px);
      overflow:visible;
    }

    .logo-svg text{
      font-family:var(--space);
      font-weight:400;
      letter-spacing:-.02em;
      text-rendering:geometricPrecision;
      paint-order:stroke fill;
    }

    .logo-fill{fill:url(#logoFillGradient)}
    .logo-base{fill:none;stroke:rgba(255,255,255,.14);stroke-width:1.7;stroke-linejoin:round;stroke-linecap:round;opacity:.95}

    .logo-glow,.logo-glow-flicker,.logo-shimmer,.glitch-layer text{
      pointer-events:none;
      mix-blend-mode:screen;
    }

    .logo-glow{
      fill:rgba(255,255,255,.92);
      opacity:.22;
      filter:blur(.35px) drop-shadow(0 0 3px rgba(255,255,255,.85)) drop-shadow(0 0 10px rgba(255,255,255,.50)) drop-shadow(0 0 22px rgba(255,255,255,.26));
      animation:brandGlow 4.8s cubic-bezier(.4,0,.6,1) infinite;
    }

    .logo-glow-flicker{fill:rgba(255,255,255,.75);opacity:0;animation:brandFlicker 6.4s ease-in-out infinite}
    .logo-shimmer{fill:url(#logoShimmerGradient)}
    .glitch-layer text{font-weight:400;letter-spacing:-.02em;opacity:0}
    .glitch-slice{fill:url(#logoFillGradient)}
    .glitch-burst-bright{fill:rgba(255,255,255,.95);filter:blur(.4px) drop-shadow(0 0 6px rgba(255,255,255,.55))}
    .glitch-burst-shadow{fill:rgba(0,0,0,.92);mix-blend-mode:multiply}
    .glitch-burst-haze{fill:rgba(200,200,205,.75);filter:blur(1.4px)}
    .glitch-ghost{fill:rgba(255,255,255,.9);filter:blur(.6px)}
    .glitch-slice-1{clip-path:inset(0 0 78% 0)}
    .glitch-slice-2{clip-path:inset(24% 0 56% 0)}
    .glitch-slice-3{clip-path:inset(52% 0 28% 0)}
    .glitch-slice-4{clip-path:inset(76% 0 0 0)}

    .logo.glitch-slice-mode .glitch-slice{opacity:1}
    .logo.glitch-slice-mode .glitch-slice-1{animation:glitchShiftA 220ms cubic-bezier(.2,1,.3,1) 1}
    .logo.glitch-slice-mode .glitch-slice-2{animation:glitchShiftB 220ms cubic-bezier(.2,1,.3,1) 1}
    .logo.glitch-slice-mode .glitch-slice-3{animation:glitchShiftC 220ms cubic-bezier(.2,1,.3,1) 1}
    .logo.glitch-slice-mode .glitch-slice-4{animation:glitchShiftD 220ms cubic-bezier(.2,1,.3,1) 1}
    .logo.glitch-chroma-mode .glitch-burst-bright{opacity:1;animation:burstBright 300ms cubic-bezier(.33,1,.68,1) 1}
    .logo.glitch-chroma-mode .glitch-burst-shadow{opacity:1;animation:burstShadow 300ms cubic-bezier(.33,1,.68,1) 1}
    .logo.glitch-chroma-mode .glitch-burst-haze{opacity:.75;animation:burstHaze 300ms cubic-bezier(.33,1,.68,1) 1}
    .logo.glitch-jitter-mode .logo-svg{animation:jitterLogo 220ms linear 1}
    .logo.glitch-ghost-mode .glitch-ghost{opacity:.55;animation:ghostTrail 260ms cubic-bezier(.22,.61,.36,1) 1}
    .logo.glitch-tear-mode .glitch-slice-2,.logo.glitch-tear-mode .glitch-slice-3{opacity:1;animation:glitchTear 200ms cubic-bezier(.68,-.55,.27,1.55) 1}

    @keyframes brandGlow{0%,100%{opacity:.18}25%{opacity:.42}50%{opacity:.72}75%{opacity:.36}}
    @keyframes brandFlicker{0%,14%,100%{opacity:0}15%{opacity:.55}16%{opacity:.08}17%{opacity:.62}19%{opacity:0}48%,54%{opacity:0}49%{opacity:.38}50%{opacity:.05}51%{opacity:.52}52%{opacity:.15}53%{opacity:.4}}
    @keyframes glitchShiftA{0%,100%{transform:translate(0) skewX(0)}20%{transform:translate(-9px,-1px) skewX(-3deg)}45%{transform:translate(13px,1px) skewX(2deg)}70%{transform:translate(-4px) skewX(-1deg)}}
    @keyframes glitchShiftB{0%,100%{transform:translate(0) skewX(0)}22%{transform:translate(14px,1px) skewX(2deg)}48%{transform:translate(-11px,-1px) skewX(-3deg)}72%{transform:translate(6px) skewX(1deg)}}
    @keyframes glitchShiftC{0%,100%{transform:translate(0) skewX(0)}25%{transform:translate(-17px,1px) skewX(-2deg)}55%{transform:translate(8px) skewX(3deg)}80%{transform:translate(-3px,-1px) skewX(-1deg)}}
    @keyframes glitchShiftD{0%,100%{transform:translate(0) skewX(0)}28%{transform:translate(11px) skewX(1deg)}52%{transform:translate(-14px,1px) skewX(-2deg)}78%{transform:translate(5px,-1px) skewX(2deg)}}
    @keyframes burstBright{0%{transform:translate(0) scale(1);opacity:.9}30%{transform:translate(-18px,1px) scale(1.02);opacity:1}60%{transform:translate(-10px,-1px) scale(1.01);opacity:.8}100%{transform:translate(0) scale(1);opacity:0}}
    @keyframes burstShadow{0%{transform:translate(0);opacity:.9}30%{transform:translate(18px,-1px);opacity:1}60%{transform:translate(10px,1px);opacity:.7}100%{transform:translate(0);opacity:0}}
    @keyframes burstHaze{0%{transform:translate(0) scale(1);opacity:.3}50%{transform:translate(0,-1px) scale(1.025);opacity:.75}100%{transform:translate(0) scale(1);opacity:0}}
    @keyframes jitterLogo{0%,100%{transform:translate(0)}15%{transform:translate(-3px,2px)}30%{transform:translate(4px,-2px)}50%{transform:translate(-2px,3px)}70%{transform:translate(3px,-1px)}85%{transform:translate(-1px,1px)}}
    @keyframes ghostTrail{0%{opacity:0;transform:translate(-22px) scale(1.025);filter:blur(1.2px)}40%{opacity:.6;transform:translate(0) scale(1);filter:blur(.6px)}100%{opacity:0;transform:translate(16px) scale(.99);filter:blur(1px)}}
    @keyframes glitchTear{0%,100%{transform:translate(0)}30%{transform:translate(-32px)}60%{transform:translate(28px)}}

    .sub{
      margin:10px 0 0;
      max-width:78ch;
      color:var(--muted);
      font-family:var(--space);
      font-size:clamp(14px,1.4vw,16px);
      line-height:1.55;
    }

    .divider{
      height:1px;
      margin:18px 0;
      background:linear-gradient(90deg,transparent,rgba(255,255,255,.12),transparent);
    }

    .actions{
      display:flex;
      gap:12px;
      align-items:stretch;
      flex-wrap:wrap;
    }

    .btn{
      position:relative;
      isolation:isolate;
      overflow:hidden;
      flex:1 1 220px;
      max-width:360px;
      min-height:52px;
      display:flex;
      align-items:center;
      justify-content:center;
      padding:14px 16px;
      border:1px solid transparent;
      border-radius:999px;
      color:rgba(255,255,255,.92);
      text-decoration:none;
      cursor:pointer;
      user-select:none;
      touch-action:manipulation;
      background:
        linear-gradient(180deg,rgba(14,15,19,.66),rgba(5,6,9,.58)) padding-box,
        linear-gradient(90deg,rgba(255,255,255,.06),rgba(255,255,255,.16),rgba(255,255,255,.06)) border-box;
      box-shadow:0 14px 38px rgba(0,0,0,.62),inset 1px 1px 0 rgba(255,255,255,.14),inset -1px -1px 0 rgba(0,0,0,.28),inset 0 0 20px rgba(255,255,255,.02);
      transition:transform .28s cubic-bezier(.16,.84,.28,1),filter .22s ease,box-shadow .22s ease;
    }

    .btn::before{--glass-blur:14px}

    .btn::after{
      content:"";
      position:absolute;
      inset:0;
      z-index:1;
      border-radius:inherit;
      pointer-events:none;
      opacity:0;
      background:radial-gradient(140px 70px at var(--mx,50%) var(--my,50%),rgba(255,255,255,.14),transparent 60%);
      transition:opacity .3s ease;
    }

    .btn:hover{
      transform:translateY(-2px);
      filter:brightness(1.12);
      box-shadow:0 18px 46px rgba(0,0,0,.68),inset 1px 1px 0 rgba(255,255,255,.18),inset -1px -1px 0 rgba(0,0,0,.28),inset 0 0 22px rgba(255,255,255,.04);
    }

    .btn:hover::after{opacity:1}
    .btn:active{transform:translateY(0) scale(.985);transition:transform .08s ease}

    .btn-inner{
      position:relative;
      z-index:2;
      width:100%;
      display:flex;
      align-items:center;
      justify-content:center;
      gap:12px;
      min-width:0;
    }

    .dot{
      flex:0 0 auto;
      width:8px;
      height:8px;
      border-radius:999px;
      background:rgba(255,255,255,.88);
      box-shadow:0 0 0 2px rgba(255,255,255,.12),0 0 10px rgba(255,255,255,.45);
      animation:dotPulse 2.4s cubic-bezier(.4,0,.2,1) infinite;
    }

    @keyframes dotPulse{0%,100%{transform:scale(1);box-shadow:0 0 0 2px rgba(255,255,255,.1),0 0 6px rgba(255,255,255,.3)}50%{transform:scale(1.22);box-shadow:0 0 0 2px rgba(255,255,255,.22),0 0 16px rgba(255,255,255,.75)}}

    .btn-text{
      min-width:0;
      display:flex;
      flex-direction:column;
      gap:4px;
      text-align:left;
    }

    .btn-title,.btn-meta{
      overflow:hidden;
      white-space:nowrap;
      text-overflow:ellipsis;
      line-height:1.05;
    }

    .btn-title{font-family:var(--space);font-weight:700;font-size:14px;color:rgba(255,255,255,.94)}
    .btn-meta{font-family:var(--mono);font-weight:700;font-size:12px;color:rgba(255,255,255,.52)}

    .features-title{
      margin:0;
      font-family:var(--space);
      font-size:16px;
      letter-spacing:.02em;
      color:rgba(255,255,255,.88);
    }

    .feature-grid{
      display:grid;
      grid-template-columns:repeat(2,1fr);
      gap:12px;
      margin-top:14px;
    }

    .feature-top{
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap:12px;
    }

    .feature-name{
      margin:0;
      font-family:var(--space);
      font-size:15px;
      letter-spacing:.01em;
      color:rgba(255,255,255,.90);
    }

    .feature-desc,.explain-text{
      font-family:var(--space);
      font-size:13px;
      line-height:1.55;
      color:rgba(255,255,255,.64);
    }

    .feature-desc{margin:10px 0 0}

    .icon{
      flex:0 0 auto;
      display:block;
      width:24px;
      height:24px;
      overflow:visible;
      opacity:.9;
      filter:drop-shadow(0 6px 14px rgba(0,0,0,.5));
    }

    .icon *{vector-effect:non-scaling-stroke}
    .card:hover .icon{filter:drop-shadow(0 6px 14px rgba(0,0,0,.5)) drop-shadow(0 0 5px rgba(255,255,255,.3))}
    .icon-perf .needle,.icon-perf .tick,.icon-stab .bar,.icon-sec .shield,.icon-arch .flame-outer,.icon-arch .flame-inner{transform-box:view-box}
    .icon-perf .needle{transform-origin:12px 12px;animation:needleSweep 4.2s cubic-bezier(.65,.05,.35,.95) infinite}
    .icon-perf .tick{transform-origin:12px 12px;animation:tickFade 4.2s ease-in-out infinite}
    .icon-perf .tick-2{animation-delay:.2s}.icon-perf .tick-3{animation-delay:.4s}
    .icon-stab .bar-1{transform-origin:6px 18px;animation:barPulse 2.8s cubic-bezier(.65,.05,.35,.95) infinite}
    .icon-stab .bar-2{transform-origin:12px 18px;animation:barPulse 2.8s cubic-bezier(.65,.05,.35,.95) .18s infinite}
    .icon-stab .bar-3{transform-origin:18px 18px;animation:barPulse 2.8s cubic-bezier(.65,.05,.35,.95) .36s infinite}
    .icon-sec .shield{transform-origin:12px 12px;stroke-dasharray:76;animation:shieldBreath 4s ease-in-out infinite}
    .icon-sec .check{stroke-dasharray:12;stroke-dashoffset:12;animation:drawCheck 4s cubic-bezier(.65,.05,.35,.95) infinite}
    .icon-arch .flame-outer{transform-origin:12px 14px;animation:flameBreath 3.2s cubic-bezier(.45,0,.55,1) infinite}
    .icon-arch .flame-inner{transform-origin:12px 15px;animation:flameFlicker 1.8s cubic-bezier(.65,.05,.35,.95) infinite}
    .card:hover .icon-perf .needle{animation-duration:1.8s}.card:hover .icon-stab .bar{animation-duration:1.4s}.card:hover .icon-sec .check{animation-duration:1.8s}.card:hover .icon-arch .flame-inner{animation-duration:.8s}

    @keyframes needleSweep{0%,100%{transform:rotate(-34deg)}30%{transform:rotate(18deg)}48%{transform:rotate(31deg)}66%{transform:rotate(-8deg)}}
    @keyframes tickFade{0%,100%{opacity:1}50%{opacity:.38}}
    @keyframes barPulse{0%,100%{transform:scaleY(.82);opacity:.76}50%{transform:scaleY(1.12);opacity:1}}
    @keyframes shieldBreath{0%,100%{opacity:.82;transform:scale(1);filter:drop-shadow(0 0 0 rgba(255,255,255,0))}50%{opacity:1;transform:scale(1.035);filter:drop-shadow(0 0 5px rgba(255,255,255,.42))}}
    @keyframes drawCheck{0%,10%{stroke-dashoffset:12;opacity:.45}30%,82%{stroke-dashoffset:0;opacity:1}96%,100%{stroke-dashoffset:12;opacity:.45}}
    @keyframes flameBreath{0%,100%{transform:scaleY(1) scaleX(1);opacity:.88}50%{transform:scaleY(1.06) scaleX(.97);opacity:1}}
    @keyframes flameFlicker{0%,100%{transform:scaleY(1) scaleX(1) translateY(0);opacity:.5}20%{transform:scaleY(1.14) scaleX(.92) translateY(-.3px);opacity:.82}45%{transform:scaleY(.92) scaleX(1.06) translateY(.2px);opacity:.58}70%{transform:scaleY(1.08) scaleX(.95) translateY(-.2px);opacity:.78}}

    .reveal{
      opacity:0;
      transform:translateY(22px) scale(.985);
      transition:opacity .7s cubic-bezier(.16,.84,.28,1),transform .7s cubic-bezier(.16,.84,.28,1);
    }
    .reveal.in{opacity:1;transform:none}
    .reveal[data-delay="1"]{transition-delay:.08s}.reveal[data-delay="2"]{transition-delay:.18s}.reveal[data-delay="3"]{transition-delay:.28s}.reveal[data-delay="4"]{transition-delay:.38s}

    :focus-visible{outline:2px solid rgba(255,255,255,.55);outline-offset:3px}

    .vhs{
      z-index:9;
      mix-blend-mode:overlay;
      opacity:.30;
      background:repeating-linear-gradient(0deg,rgba(255,255,255,.045) 0 1px,transparent 1px 3px);
      animation:vhsLinesJitter 4s ease-in-out infinite;
    }

    .vhs::after{
      content:"";
      position:absolute;
      left:0;
      right:0;
      top:-140px;
      height:140px;
      background:linear-gradient(180deg,transparent,rgba(255,255,255,.035) 30%,rgba(255,255,255,.08) 50%,rgba(255,255,255,.035) 70%,transparent);
      filter:blur(.5px);
      animation:vhsScan 11s cubic-bezier(.45,.05,.55,.95) infinite;
    }

    .vhs-glitch{
      z-index:11;
      opacity:0;
      mix-blend-mode:screen;
      background:linear-gradient(180deg,transparent 0 18%,rgba(255,255,255,.06) 18.2% 19.2%,transparent 19.4% 44%,rgba(255,255,255,.09) 44.2% 45%,transparent 45.2% 72%,rgba(255,255,255,.05) 72.2% 73.4%,transparent 73.6%);
      animation:vhsGlitch 12s cubic-bezier(.3,0,.7,1) infinite;
    }

    @keyframes vhsScan{0%{transform:translateY(0);opacity:.3}10%,90%{opacity:1}100%{transform:translateY(calc(100vh + 280px));opacity:.3}}
    @keyframes vhsLinesJitter{0%,100%{transform:translateY(0)}25%{transform:translateY(.3px)}50%{transform:translateY(-.3px)}75%{transform:translateY(.15px)}}
    @keyframes vhsGlitch{0%,95%,100%{opacity:0;transform:translateX(0)}95.8%{opacity:.5;transform:translateX(-3px)}96.4%{opacity:.25;transform:translateX(2px)}97.2%{opacity:.4;transform:translateX(-1px)}98%{opacity:.1;transform:translateX(0)}}

    .explain-toggle{
      margin-top:10px;
      padding:0;
      border:0;
      background:none;
      color:rgba(255,255,255,.50);
      font-family:var(--space);
      font-size:12px;
      line-height:1.2;
      cursor:pointer;
      text-decoration:underline;
      text-underline-offset:2px;
      transition:color .16s ease,opacity .16s ease;
    }

    .explain-toggle:hover{color:rgba(255,255,255,.78)}
    .explain-toggle:active{opacity:.8}

    .explain-text{
      max-width:34ch;
      margin:8px 0 0;
      font-size:12px;
      color:rgba(255,255,255,.56);
    }

    .explain-text[hidden]{display:none}

    @media (max-width:820px){
      :root{--glass-blur:11px;--glass-blur-soft:8px;--shadow:0 12px 34px rgba(0,0,0,.62)}
      .feature-grid{grid-template-columns:1fr}
      .btn{max-width:none}
      .card{animation:none;background-position:0 0,50% 0}
      .icon,.card:hover .icon{filter:none}
      .logo-glow{filter:blur(.3px) drop-shadow(0 0 3px rgba(255,255,255,.8)) drop-shadow(0 0 10px rgba(255,255,255,.45))}
      .vhs{opacity:.24;animation:none}
      .vhs::after,.vhs-glitch{display:none}
    }

    @media (max-width:520px){
      :root{--glass-blur:9px;--glass-blur-soft:7px}
      .frame,.card{border-radius:18px}
      .frame-inner{padding:16px}
      .stack{gap:12px}
      .divider{margin:16px 0}
      .actions{gap:10px}
      .btn{min-height:48px;padding:12px 14px}
      .feature-grid{gap:10px}
      .card{min-height:108px;padding:14px}
      .logo-svg{height:clamp(32px,10vw,42px)}
      .sub{font-size:13px}.features-title{font-size:15px}.feature-name{font-size:14px}.feature-desc{font-size:12px}
      .icon-perf .needle,.icon-perf .tick,.icon-stab .bar,.icon-sec .shield,.icon-sec .check,.icon-arch .flame-outer,.icon-arch .flame-inner{animation:none}
      .icon-sec .check{stroke-dashoffset:0;opacity:1}
      .logo-shimmer{display:none}
    }

    @media (pointer:coarse){
      .btn:hover{transform:none;filter:none;box-shadow:0 14px 38px rgba(0,0,0,.62),inset 1px 1px 0 rgba(255,255,255,.14),inset -1px -1px 0 rgba(0,0,0,.28),inset 0 0 20px rgba(255,255,255,.02)}
      .btn:hover::after{opacity:0}
      .card:hover{transform:none}
      .card:hover .icon{filter:drop-shadow(0 6px 14px rgba(0,0,0,.5))}
      .card:hover .icon-perf .needle{animation-duration:4.2s}.card:hover .icon-stab .bar{animation-duration:2.8s}.card:hover .icon-sec .check{animation-duration:4s}.card:hover .icon-arch .flame-inner{animation-duration:1.8s}
    }

    @media (prefers-reduced-motion:reduce){
      *,*::before,*::after{
        animation:none!important;
        transition:none!important;
        scroll-behavior:auto!important;
      }
      .hero,.hero .logo,.hero .sub,.hero .divider,.hero .actions>.btn,.reveal{opacity:1;transform:none;filter:none}
      .frame,.card{background-position:0 0,52% 0}
      .glitch-layer,.vhs-glitch{display:none}
      .icon-sec .check{stroke-dashoffset:0}
    }
  </style>
</head>

<body>
  <canvas id="bg" aria-hidden="true"></canvas>
  <div class="vignette" aria-hidden="true"></div>

  <main class="stage">
    <div class="stack" aria-label="Главное меню">
      <section class="frame hero">
        <div class="frame-inner">
          <div class="logo" aria-label="Ethereal">
            <svg class="logo-svg" viewBox="0 0 820 120" preserveAspectRatio="xMinYMid meet" aria-hidden="true">
              <defs>
                <linearGradient id="logoFillGradient" x1="0%" y1="0%" x2="0%" y2="100%">
                  <stop offset="0%" stop-color="#fff"></stop>
                  <stop offset="48%" stop-color="#b8b8b8"></stop>
                  <stop offset="100%" stop-color="#3e3e3e"></stop>
                </linearGradient>
                <linearGradient id="logoShimmerGradient" gradientUnits="userSpaceOnUse" x1="-300" y1="0" x2="-100" y2="0">
                  <stop offset="0%" stop-color="rgba(255,255,255,0)"></stop>
                  <stop offset="42%" stop-color="rgba(255,255,255,0)"></stop>
                  <stop offset="50%" stop-color="rgba(255,255,255,.95)"></stop>
                  <stop offset="58%" stop-color="rgba(255,255,255,0)"></stop>
                  <stop offset="100%" stop-color="rgba(255,255,255,0)"></stop>
                  <animate attributeName="x1" values="-300;1020;1020;-300" keyTimes="0;.35;1;1" dur="6.2s" repeatCount="indefinite"></animate>
                  <animate attributeName="x2" values="-100;1220;1220;-100" keyTimes="0;.35;1;1" dur="6.2s" repeatCount="indefinite"></animate>
                </linearGradient>
              </defs>

              <text class="logo-glow" x="0" y="94" font-size="96">Ethereal</text>
              <text class="logo-fill" x="0" y="94" font-size="96">Ethereal</text>
              <text class="logo-base" x="0" y="94" font-size="96">Ethereal</text>
              <text class="logo-glow-flicker" x="0" y="94" font-size="96">Ethereal</text>
              <text class="logo-shimmer" x="0" y="94" font-size="96">Ethereal</text>

              <g class="glitch-layer" aria-hidden="true">
                <text class="glitch-slice glitch-slice-1" x="0" y="94" font-size="96">Ethereal</text>
                <text class="glitch-slice glitch-slice-2" x="0" y="94" font-size="96">Ethereal</text>
                <text class="glitch-slice glitch-slice-3" x="0" y="94" font-size="96">Ethereal</text>
                <text class="glitch-slice glitch-slice-4" x="0" y="94" font-size="96">Ethereal</text>
                <text class="glitch-burst-bright" x="0" y="94" font-size="96">Ethereal</text>
                <text class="glitch-burst-shadow" x="0" y="94" font-size="96">Ethereal</text>
                <text class="glitch-burst-haze" x="0" y="94" font-size="96">Ethereal</text>
                <text class="glitch-ghost" x="0" y="94" font-size="96">Ethereal</text>
              </g>
            </svg>
          </div>

          <p class="sub">Никаких следов. Лишь результат.</p>
          <div class="divider"></div>

          <nav class="actions" aria-label="Ссылки">
            <a class="btn" href="https://files.ethereal.pw" aria-label="Скачать">
              <span class="btn-inner"><span class="dot" aria-hidden="true"></span><span class="btn-text"><span class="btn-title">Установить</span><span class="btn-meta">files.ethereal.pw</span></span></span>
            </a>
            <a class="btn" href="https://status.ethereal.pw" aria-label="Статус">
              <span class="btn-inner"><span class="dot" aria-hidden="true"></span><span class="btn-text"><span class="btn-title">Статус</span><span class="btn-meta">status.ethereal.pw</span></span></span>
            </a>
            <a class="btn" href="https://t.me/pastereal" aria-label="Telegram">
              <span class="btn-inner"><span class="dot" aria-hidden="true"></span><span class="btn-text"><span class="btn-title">Телеграм канал</span><span class="btn-meta">t.me/pastereal</span></span></span>
            </a>
          </nav>
        </div>
      </section>

      <section class="frame">
        <div class="frame-inner">
          <h2 class="features-title">Фишки</h2>
          <div class="divider"></div>

          <div class="feature-grid" id="featureGrid">
            <article class="card reveal" data-delay="1">
              <div class="feature-top">
                <h3 class="feature-name">Производительность</h3>
                <svg class="icon icon-perf" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path class="tick tick-1" d="M12 3v4" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linecap="round"></path>
                  <path class="tick tick-2" d="M7.2 5.3l2.2 3.4" stroke="rgba(255,255,255,.55)" stroke-width="2" stroke-linecap="round"></path>
                  <path class="tick tick-3" d="M16.8 5.3l-2.2 3.4" stroke="rgba(255,255,255,.55)" stroke-width="2" stroke-linecap="round"></path>
                  <path d="M4.5 14.5a7.5 7.5 0 0 1 15 0" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linecap="round"></path>
                  <path class="needle" d="M12 12l4.2-2.2" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linecap="round"></path>
                </svg>
              </div>
              <p class="feature-desc">Все копируют. Мы — оптимизируем.</p>
            </article>

            <article class="card reveal" data-delay="2">
              <div class="feature-top">
                <h3 class="feature-name">Стабильность</h3>
                <svg class="icon icon-stab" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path class="bar bar-1" d="M6 18V10" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linecap="round"></path>
                  <path class="bar bar-2" d="M12 18V6" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linecap="round"></path>
                  <path class="bar bar-3" d="M18 18v-8" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linecap="round"></path>
                  <path d="M4 18h16" stroke="rgba(255,255,255,.55)" stroke-width="2" stroke-linecap="round"></path>
                </svg>
              </div>
              <p class="feature-desc">Собрано так, чтобы не разваливаться от каждого чиха.</p>
            </article>

            <article class="card reveal" data-delay="3">
              <div class="feature-top">
                <h3 class="feature-name">Безопасность</h3>
                <svg class="icon icon-sec" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path class="shield" d="M12 3l7 4v6c0 5-3 8-7 8s-7-3-7-8V7l7-4z" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linejoin="round"></path>
                  <path class="check" d="M9.5 12l1.8 1.8L15.8 9.3" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"></path>
                </svg>
              </div>
              <p class="feature-desc">Мы возвращаем nullptr там, где другие ловят UB.</p>
              <button class="explain-toggle" type="button" aria-expanded="false" aria-controls="explain-security">простыми словами</button>
              <p class="explain-text" id="explain-security" hidden>Перевод: если что-то пошло не так, софт это безопасно проглатывает, а не ломается.</p>
            </article>

            <article class="card reveal" data-delay="4">
              <div class="feature-top">
                <h3 class="feature-name">Архитектура</h3>
                <svg class="icon icon-arch" viewBox="0 0 24 24" fill="none" aria-hidden="true">
                  <path class="flame-outer" d="M12 2c3.5 3 6 6.3 6 10a6 6 0 1 1-12 0c0-3.7 2.5-7 6-10z" stroke="rgba(255,255,255,.88)" stroke-width="2" stroke-linejoin="round"></path>
                  <path class="flame-inner" d="M9 14c.8 1.2 2.1 2 3.6 2 1.2 0 2.3-.4 3.1-1.2" stroke="rgba(255,255,255,.55)" stroke-width="2" stroke-linecap="round"></path>
                </svg>
              </div>
              <p class="feature-desc">Части проекта связаны нормально, а не держатся на оффсетах из воздуха.</p>
              <button class="explain-toggle" type="button" aria-expanded="false" aria-controls="explain-architecture">простыми словами</button>
              <p class="explain-text" id="explain-architecture" hidden>Перевод: пока у некоторых всё в main.cpp и через жопу, у нас — наоборот: разделено на отдельные файлы и написано аккуратно.</p>
            </article>
          </div>
        </div>
      </section>
    </div>
  </main>

  <div class="vhs" aria-hidden="true"></div>
  <div class="vhs-glitch" aria-hidden="true"></div>

  <script>
    (() => {
      "use strict";

      const prefersReducedMotion = matchMedia("(prefers-reduced-motion: reduce)").matches;
      const mqMobile = matchMedia("(max-width: 820px)");
      const mqSmall = matchMedia("(max-width: 520px)");
      const finePointer = matchMedia("(pointer: fine)").matches;

      if(finePointer){
        document.querySelectorAll(".btn").forEach((btn) => {
          btn.addEventListener("pointermove", (event) => {
            const rect = btn.getBoundingClientRect();
            btn.style.setProperty("--mx", `${((event.clientX - rect.left) / rect.width) * 100}%`);
            btn.style.setProperty("--my", `${((event.clientY - rect.top) / rect.height) * 100}%`);
          }, { passive:true });
        });
      }

      const revealItems = document.querySelectorAll(".reveal");
      if(!prefersReducedMotion && "IntersectionObserver" in window){
        const observer = new IntersectionObserver((entries) => {
          for(const entry of entries){
            if(!entry.isIntersecting) continue;
            entry.target.classList.add("in");
            observer.unobserve(entry.target);
          }
        }, { threshold:.18, rootMargin:"60px 0px" });
        revealItems.forEach((item) => observer.observe(item));
      }else{
        revealItems.forEach((item) => item.classList.add("in"));
      }

      document.addEventListener("click", (event) => {
        const btn = event.target.closest(".explain-toggle");
        if(!btn) return;
        const text = document.getElementById(btn.getAttribute("aria-controls"));
        if(!text) return;
        const expanded = btn.getAttribute("aria-expanded") === "true";
        btn.setAttribute("aria-expanded", String(!expanded));
        text.hidden = expanded;
      });

      const logo = document.querySelector(".logo");
      if(logo && !prefersReducedMotion){
        const desktopModes = [
          ["glitch-slice-mode"],["glitch-chroma-mode"],["glitch-jitter-mode"],["glitch-ghost-mode"],["glitch-tear-mode"],
          ["glitch-slice-mode","glitch-chroma-mode"],["glitch-jitter-mode","glitch-chroma-mode"],["glitch-slice-mode","glitch-jitter-mode"],["glitch-ghost-mode","glitch-chroma-mode"],["glitch-tear-mode","glitch-chroma-mode","glitch-jitter-mode"]
        ];
        const mobileModes = [["glitch-slice-mode"],["glitch-chroma-mode"],["glitch-jitter-mode"],["glitch-tear-mode"],["glitch-slice-mode","glitch-jitter-mode"]];
        const modes = mqMobile.matches ? mobileModes : desktopModes;
        const rand = (min, max) => min + Math.random() * (max - min);
        const pick = (array) => array[Math.floor(Math.random() * array.length)];

        const runOnce = (done) => {
          if(document.hidden){ done?.(); return; }
          const chosen = pick(modes);
          chosen.forEach((name) => logo.classList.add(name));
          setTimeout(() => {
            chosen.forEach((name) => logo.classList.remove(name));
            done?.();
          }, rand(140, mqMobile.matches ? 260 : 320));
        };

        const schedule = () => {
          setTimeout(() => {
            if(document.hidden){ schedule(); return; }
            const burst = Math.random();
            const tripleChance = mqMobile.matches ? 0 : .18;
            const doubleChance = mqMobile.matches ? .18 : .32;

            if(burst < tripleChance){
              runOnce(() => setTimeout(() => runOnce(() => Math.random() < .4 ? setTimeout(() => runOnce(schedule), rand(60,160)) : schedule()), rand(60,180)));
            }else if(burst < doubleChance){
              runOnce(() => setTimeout(() => runOnce(schedule), rand(80,200)));
            }else{
              runOnce(schedule);
            }
          }, mqMobile.matches ? rand(2800,9000) : rand(1400,6800));
        };

        setTimeout(schedule, 2400);
      }

      const canvas = document.getElementById("bg");
      const ctx = canvas?.getContext("2d", { alpha:false, desynchronized:true });
      if(!ctx) return;

      const staticLayer = document.createElement("canvas");
      const staticCtx = staticLayer.getContext("2d", { alpha:false });

      let dpr = 1;
      let width = 0;
      let height = 0;
      let stars = [];
      let clusters = [];
      let nebulae = [];
      let bands = [];
      let twinkleStars = [];
      let lastTime = 0;
      let running = !document.hidden;
      let resizeQueued = false;

      const clamp = (v, min, max) => Math.max(min, Math.min(max, v));
      const rand = (min, max) => min + Math.random() * (max - min);
      const randi = (min, max) => Math.floor(rand(min, max + 1));

      const gauss = () => {
        let u = 0;
        let v = 0;
        while(u === 0) u = Math.random();
        while(v === 0) v = Math.random();
        return Math.sqrt(-2 * Math.log(u)) * Math.cos(Math.PI * 2 * v);
      };

      function makeStar(x = rand(0, width), y = rand(0, height), kind){
        const pick = kind || (Math.random() > .994 ? "bright" : (Math.random() > .90 ? "mid" : "micro"));
        const r = pick === "micro" ? rand(.18,.58) : (pick === "mid" ? rand(.6,1.1) : rand(1.2,2.4));
        return {
          x,y,r,
          halo:pick === "bright" ? rand(r * 4.2, r * 7.5) : (pick === "mid" ? rand(r * 1.8, r * 3.4) : 0),
          a:pick === "bright" ? rand(.82,1) : (pick === "mid" ? rand(.38,.72) : rand(.08,.38)),
          pulse:pick === "bright" ? rand(.18,.42) : rand(.05,.16),
          phase:rand(0,Math.PI * 2),
          grey:randi(190,255),
          kind:pick
        };
      }

      function buildCluster(cx, cy, radius, amount, bias){
        const list = [];
        for(let i = 0; i < amount; i++){
          const g1 = gauss();
          const g2 = gauss();
          const x = cx + g1 * radius + g2 * radius * .18;
          const y = cy + g2 * radius * rand(.36,.88) + g1 * bias;
          if(x < -20 || x > width + 20 || y < -20 || y > height + 20) continue;
          const p = Math.random();
          const star = makeStar(x, y, p > .996 ? "bright" : (p > .92 ? "mid" : "micro"));
          star.a *= rand(.88,1.16);
          star.halo *= rand(.82,1.22);
          list.push(star);
        }
        return list;
      }

      function buildScene(){
        const area = width * height;
        const fieldCount = clamp(
          Math.floor(area / (mqMobile.matches ? 1250 : 950)),
          mqSmall.matches ? 340 : 700,
          mqMobile.matches ? 1350 : 2400
        );

        stars = Array.from({ length:fieldCount }, () => makeStar());
        clusters = [];

        const clusterCount = mqSmall.matches ? 4 : (mqMobile.matches ? 7 : 10);
        for(let i = 0; i < clusterCount; i++){
          const radius = rand(Math.min(width,height) * .018, Math.min(width,height) * .068);
          clusters.push({
            stars:buildCluster(
              rand(width * .05, width * .95),
              rand(height * .04, height * .96),
              radius,
              randi(mqSmall.matches ? 34 : 58, mqMobile.matches ? 110 : 180),
              rand(-radius * .25, radius * .25)
            )
          });
        }

        bands = [
          {cx:width*.22,cy:height*.24,rx:width*.24,ry:height*.09,angle:-.42,density:.030},
          {cx:width*.72,cy:height*.68,rx:width*.19,ry:height*.065,angle:.36,density:.026},
          {cx:width*.54,cy:height*.48,rx:width*.30,ry:height*.08,angle:-.10,density:.016}
        ];

        nebulae = [
          {x:width*.18,y:height*.72,rx:width*.24,ry:height*.16,alpha:.050,rot:rand(-1.4,1.4)},
          {x:width*.78,y:height*.18,rx:width*.22,ry:height*.15,alpha:.042,rot:rand(-1.4,1.4)},
          {x:width*.54,y:height*.42,rx:width*.30,ry:height*.12,alpha:.030,rot:rand(-1.4,1.4)},
          {x:width*.82,y:height*.80,rx:width*.26,ry:height*.18,alpha:.028,rot:rand(-1.4,1.4)}
        ];

        const maxTwinkle = mqSmall.matches ? 24 : (mqMobile.matches ? 42 : 70);
        twinkleStars = stars
          .concat(...clusters.map((cluster) => cluster.stars))
          .filter((star) => star.kind !== "micro")
          .sort((a,b) => b.a - a.a)
          .slice(0, maxTwinkle);
      }

      function drawBackground(c){
        const g = c.createRadialGradient(width*.5,height*.45,0,width*.5,height*.45,Math.max(width,height)*.9);
        g.addColorStop(0,"#090909");
        g.addColorStop(.45,"#040404");
        g.addColorStop(1,"#000");
        c.fillStyle = g;
        c.fillRect(0,0,width,height);

        c.save();
        c.globalCompositeOperation = "screen";
        for(const [x,y,r,a] of [[width*.16,height*.22,Math.max(width,height)*.36,.038],[width*.62,height*.58,Math.max(width,height)*.42,.026],[width*.88,height*.16,Math.max(width,height)*.22,.034],[width*.10,height*.88,Math.max(width,height)*.26,.022]]){
          const pool = c.createRadialGradient(x,y,0,x,y,r);
          pool.addColorStop(0,`rgba(255,255,255,${a})`);
          pool.addColorStop(.34,`rgba(175,175,175,${a*.36})`);
          pool.addColorStop(1,"rgba(0,0,0,0)");
          c.fillStyle = pool;
          c.fillRect(0,0,width,height);
        }
        c.restore();
      }

      function drawNebulae(c){
        c.save();
        c.globalCompositeOperation = "screen";
        for(const n of nebulae){
          c.save();
          c.translate(n.x,n.y);
          c.rotate(n.rot);
          c.scale(n.rx,n.ry);
          const g = c.createRadialGradient(0,0,0,0,0,1);
          g.addColorStop(0,`rgba(255,255,255,${n.alpha*.9})`);
          g.addColorStop(.28,`rgba(170,170,170,${n.alpha*.45})`);
          g.addColorStop(.58,`rgba(120,120,120,${n.alpha*.22})`);
          g.addColorStop(1,"rgba(0,0,0,0)");
          c.fillStyle = g;
          c.beginPath();
          c.arc(0,0,1,0,Math.PI*2);
          c.fill();
          c.restore();
        }
        c.restore();
      }

      function drawBands(c){
        c.save();
        c.globalCompositeOperation = "screen";
        for(const b of bands){
          c.save();
          c.translate(b.cx,b.cy);
          c.rotate(b.angle);
          const g = c.createRadialGradient(0,0,0,0,0,b.rx);
          g.addColorStop(0,`rgba(255,255,255,${b.density*2.8})`);
          g.addColorStop(.26,`rgba(215,215,215,${b.density*1.25})`);
          g.addColorStop(.52,`rgba(155,155,155,${b.density*.52})`);
          g.addColorStop(1,"rgba(0,0,0,0)");
          c.fillStyle = g;
          c.scale(1,b.ry / b.rx);
          c.beginPath();
          c.arc(0,0,b.rx,0,Math.PI*2);
          c.fill();
          c.restore();
        }
        c.restore();
      }

      function drawStar(c, star, alpha = star.a, halo = true){
        const a = clamp(alpha,0,1);
        const g = star.grey;

        if(halo && star.kind !== "micro" && star.halo > 1 && a > .12){
          const h = c.createRadialGradient(star.x,star.y,0,star.x,star.y,star.halo);
          h.addColorStop(0,`rgba(${g},${g},${g},${a * (star.kind === "bright" ? .14 : .055)})`);
          h.addColorStop(.35,`rgba(${g},${g},${g},${a * (star.kind === "bright" ? .055 : .022)})`);
          h.addColorStop(1,"rgba(0,0,0,0)");
          c.fillStyle = h;
          c.beginPath();
          c.arc(star.x,star.y,star.halo,0,Math.PI*2);
          c.fill();
        }

        c.fillStyle = `rgba(${g},${g},${g},${a})`;
        if(star.r < .7) c.fillRect(star.x,star.y,1,1);
        else{
          c.beginPath();
          c.arc(star.x,star.y,star.r,0,Math.PI*2);
          c.fill();
        }

        if(star.kind === "bright"){
          c.strokeStyle = `rgba(${g},${g},${g},${a*.18})`;
          c.lineWidth = Math.max(.3, star.r * .20);
          c.beginPath();
          c.moveTo(star.x - star.halo*.34, star.y);
          c.lineTo(star.x + star.halo*.34, star.y);
          c.moveTo(star.x, star.y - star.halo*.25);
          c.lineTo(star.x, star.y + star.halo*.25);
          c.stroke();
        }
      }

      function renderStaticLayer(){
        drawBackground(staticCtx);
        drawNebulae(staticCtx);
        drawBands(staticCtx);

        staticCtx.save();
        staticCtx.globalCompositeOperation = "screen";
        for(const star of stars) drawStar(staticCtx, star, star.a, true);
        for(const cluster of clusters) for(const star of cluster.stars) drawStar(staticCtx, star, star.a, true);
        staticCtx.restore();

        const veil = staticCtx.createLinearGradient(0,0,0,height);
        veil.addColorStop(0,"rgba(0,0,0,.06)");
        veil.addColorStop(.4,"rgba(0,0,0,.02)");
        veil.addColorStop(1,"rgba(0,0,0,.12)");
        staticCtx.fillStyle = veil;
        staticCtx.fillRect(0,0,width,height);

        const vignette = staticCtx.createRadialGradient(width*.5,height*.46,Math.min(width,height)*.2,width*.5,height*.46,Math.max(width,height)*.74);
        vignette.addColorStop(0,"rgba(0,0,0,0)");
        vignette.addColorStop(.68,"rgba(0,0,0,.12)");
        vignette.addColorStop(1,"rgba(0,0,0,.42)");
        staticCtx.fillStyle = vignette;
        staticCtx.fillRect(0,0,width,height);
      }

      function render(time){
        ctx.drawImage(staticLayer,0,0,width,height);
        if(prefersReducedMotion) return;

        ctx.save();
        ctx.globalCompositeOperation = "screen";
        for(const star of twinkleStars){
          const pulse = 1 + Math.sin(time * .001 * star.pulse + star.phase) * (star.kind === "bright" ? .16 : .08);
          drawStar(ctx, star, star.a * pulse, star.kind === "bright");
        }
        ctx.restore();
      }

      function resize(){
        const cap = mqSmall.matches ? 1 : (mqMobile.matches ? 1.1 : 1.25);
        dpr = Math.min(cap, devicePixelRatio || 1);
        width = innerWidth;
        height = innerHeight;

        canvas.width = Math.floor(width * dpr);
        canvas.height = Math.floor(height * dpr);
        canvas.style.width = `${width}px`;
        canvas.style.height = `${height}px`;
        ctx.setTransform(dpr,0,0,dpr,0,0);

        staticLayer.width = canvas.width;
        staticLayer.height = canvas.height;
        staticCtx.setTransform(dpr,0,0,dpr,0,0);

        buildScene();
        renderStaticLayer();
        render(performance.now());
      }

      function tick(time){
        requestAnimationFrame(tick);
        if(!running || prefersReducedMotion || mqSmall.matches) return;

        const fps = mqMobile.matches ? 22 : 28;
        const minDelta = 1000 / fps;
        if(!lastTime) lastTime = time;
        if(time - lastTime < minDelta) return;
        lastTime = time;
        render(time);
      }

      addEventListener("resize", () => {
        if(resizeQueued) return;
        resizeQueued = true;
        requestAnimationFrame(() => {
          resizeQueued = false;
          resize();
        });
      }, { passive:true });

      document.addEventListener("visibilitychange", () => {
        running = !document.hidden;
        if(running) lastTime = 0;
      });

      resize();
      requestAnimationFrame(tick);
    })();
  </script>
</body>
</html>
